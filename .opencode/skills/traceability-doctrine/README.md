# traceability-doctrine

Portable traceability rules for Axiom.

Use
- Load this skill when you need to add or audit `axiom:trace ...` markers across PRDs/specs/plans/code/tests/docs/evidence.
- Includes L0 `prd → spec` link rules (PRDs in `prds/` are the intent layer above specs).
