# evidence-bundle-schema

Portable evidence bundle schema and validation rules.

Use
- Load this skill when writing `.memory-bank/work-items/<ID>/verification.md` and `runs/<RUN_ID>/verification.md`.
