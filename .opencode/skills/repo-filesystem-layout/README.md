# repo-filesystem-layout

Portable guidance for auditing and documenting a repository filesystem layout (where files belong, and how to avoid layout drift).

Use
- Load this skill when an agent needs to add new files or directories and you want consistent placement rules.
- Load this skill when you want to (re)generate a repo layout spec (for example `specs/18-Repo-Filesystem-Layout.md`).
