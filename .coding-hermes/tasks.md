
## Dogfood Findings (2026-09-01)
Verdict: PROMISING-BUT-ROUGH
Promise: {"entry_point":"CLI binary  (cmd/consensus, subcommands init/serve/mcp-stdio) that boots an HTTP server on :8090 exposing the REST API /api/v1 (sessions, memory, tasks, billing), SSE event stream, MCP server (SSE + stdio), embedded OpenAPI spec, and Chronicle dashboard at /chronicle/;

- [P0] LLM path dead on fresh checkout: shipped consensus.yaml pins base_url to DeepSeek and defeats both documented env overrides — With the shipped consensus.yaml present, OPENROUTER_API_KEY swaps the key but calls still hit api.deepseek.com and 401, contradicting the README's 'no separate CONSENSUS_LLM_BASE_URL required'; CONSEN
- [P1] POST /api/v1/sessions 400s on two undocumented required fields (agent_name, goal) — The documented curl example in docs/API.md returns 400; both agent_name and goal are required but absent from the docs. Creating the first session took two trial-and-error 400s — the documented quicks
- [P1] Message contract is async but docs promise a synchronous response; no poll-for-response recipe exists — POST /api/v1/sessions/{id}/message returns {"status":"message_received"} instantly while docs say it 'returns the agent response'. The agent reply is never observable via any documented path — SSE /ap
- [P2] MCP SSE routes undiscoverable: /api/v1/mcp 404s, /mcp is a 501 stub, real routes live only in docs/INTEGRATION.md — Route guessing cost real time; /mcp/sse + /mcp/message + _meta.authorization work once found (SSE handshake, tools/list, stdio auth error all verified) but are absent from the README — exactly the pro
- [P2] Free-port guidance is stale and its '404 page not found' tell-tale misses health-looking impostors — README's suggested ports 8090/8095/8091 were all occupied on this host (4 ports probed before 8124 worked), and the 8095 occupant answered /api/v1/health with plausible JSON (status:unhealthy, version

## Dogfood Findings (2026-09-03)
Verdict: PROMISING-BUT-ROUGH
Promise: "The database is the runtime" — REST/CLI/MCP harness where agent context is a live SQL view, memory is an append-only ACID ledger, sessions survive kill -9; a user sends a message via POST /api/v1/sessions/{id}/message and gets the agent response.

Reality: infra surface is solid and pleasant (init→serve→health ~2 min incl. build; auth, SSE, MCP, /doc/api all working; kill -9 recovery verified; goal-driven autonomous SQL execution WORKS with real DeepSeek calls). But the documented conversational path is dead: the user message never reaches the LLM (same 2-message prompt every turn), no assistant reply is ever produced or observable, tokens stay 0 after ~30 real calls, and the README's recommended docker quickstart image is not anonymously pullable.

- [P0] DF-CONSENSUS-6 — Conversational message path broken end-to-end: user message never fed to the LLM (server log shows messages=2 every turn across ~30 calls), model itself reports "No user-supplied text is present in this turn", no assistant reply anywhere (memory/agent_messages/iterations/SSE/shim all empty), tokens_used stay 0, agent_billing/audit_logs/iteration_commits all 0 rows after 4 runs. A/B: goal-driven task + {"type":"user_instruction"} wake message WORKS (staged SQL executed, memory event landed) — docs/API.md documents the broken path as "returns the agent response".
- [P0] DF-CONSENSUS-7 — README recommended quickstart fails for a fresh user: `docker pull ghcr.io/wojons/consensus:latest` → denied (401 anonymous; alpine pulls fine from same agent), and the GitHub repo is not anonymously cloneable either. Neither documented install path works from zero. Do NOT widen visibility — publish the image via CI instead. Verified fallback with repo access: tar tree → bootstrap Go 1.26.5 → `go build` 61s → init/serve/health 200 PASS.
- [P1] DF-CONSENSUS-8 — Crash recovery is real but recovers almost nothing: kill -9 mid-loop → restart → session/messages intact and heartbeat auto-resumes (proven), yet in-flight runs emit zero durable output, so "session data intact" = the user's own messages survived; staging_buffer stranded 74 staged commands, iteration_commits=0.
- [P2] DF-CONSENSUS-9 — Install docs assume toolchains (no Go, no sudo on a bare agent) and miss the bunkerd double-dockerd lock landmine; POST /sessions ignores requested model (returns "default"); one duplicate user_message row observed (send + heartbeat-resume race).

Install (bunker, mandatory leg): bunker-las-03 spawn blocked (orphaned port-range pool, bunkerd restart needs interactive approval — same class as qa-foreman-ops/spawn-pool-exhaustion-2026-09-03); ran on bunker-las-01 instead. agent=af56fcf2, destroyed OK. Smoke: PASS (health 200 status:ok, v0.1.0, sqlite, schema 23).

## Dogfood Findings (2026-09-04)
Verdict: PROMISING-BUT-ROUGH
Promise: {"entry_point":"Go CLI binary `consensus` (cmd/consensus) — subcommands init/serve/mcp-stdio/tool/skill — serving an HTTP server on :8090 (REST /api/v1, SSE /api/v1/events, MCP /mcp/sse, opencode shim, /chronicle dashboard); also distributed as Docker image ghcr.io/wojons/consensus:latest.","promise

- [P0] Invalid LLM key wedges the entire API until restart — Message POST with a dummy LLM key triggered 'begin tx: context deadline exceeded' in the harness planning loop; /api/v1/health then timed out and the whole server was unresponsive until restart. This is a normal user error path (typo'd/expired DEEPSEEK_API_KEY) with no documented recovery, and it directly strains the promised 'ACID-committed with crash recovery' and 'circuit breakers cap LLM spend' claims — the breaker did not contain the failure.
- [P1] CLI tool/skill subcommands never authenticate — `consensus tool list` and `skill list` always fail 'UNAUTHENTICATED: missing API key' — --api-key flag, CONSENSUS_API_KEY env, and config file are all ignored (server logs show the 401s). Two of the five promised subcommands are unusable via any documented mechanism.
- [P1] Docker distribution path broken — image cannot be pulled — `docker pull ghcr.io/wojons/consensus:latest` returns 'denied' from the registry, so the documented `docker run` command is unusable without ghcr auth. The promise explicitly distributes via this image; no auth instructions are provided.
- [P2] Documented session-create curl example fails as written — The docs' POST /api/v1/sessions example 400s twice because agent_name and goal are required but omitted from the documented curl. Core session flow works once the fields are supplied, but the quickstart example is a dead end for new users.
- [P2] CLI flag and port friction across serve/mcp-stdio — `serve` rejects --llm-key with a bare 'unknown flag' (no hint that it's init-only or that DEEPSEEK_API_KEY is the way); mcp-stdio initialize fails without --api-key though the README example shows none; port-occupied 404 symptom was documented but still cost time to identify and re-port.
