# GitReins — gitleaks configuration
# Auto-generated by 'gitreins init'. Edit to add project-specific exclusions.
# See: https://github.com/gitleaks/gitleaks#configuration

[allowlist]
description = "Auto-generated by gitreins init — skip dependency dirs, docs, specs"

paths = [
  '''\.git/''',
  '''\.gitreins/''',
  '''\.gitreins/history/''',
  '''.*\.log''',
  '''vendor/''',
  '''specs/''',
  '''docs/''',
  '''.*\.spec\.md''',
  '''.*\.md''',
]

# Consensus test keys use cs_sk_ prefix with "test" in the key body.
# Real production keys never contain "test", so this is safe.
regexes = [
  '''cs_sk_.*test.*''',
]
