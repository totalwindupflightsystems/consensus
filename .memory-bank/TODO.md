# TODO

Working execution backlog. Items prioritized by impact.

## Active

- [x] `runtime-serve-bootstrap-test-01` — Add automated regression coverage for the full `conscience serve` bootstrap/auth path. (16/16 bootstrap tests pass; 22/22 packages pass; all ACs verified)
- [x] `bootstrap-output-stream-01` — Normalize or document first-admin-key output streams for `init` and `serve`. (commit a34fe4d)
- [x] `postgres-bootstrap-verification-01` — Add gated Postgres bootstrap verification or record accepted environment gap. (Gated tests added, accepted environment gap documented)
- [x] `bootstrap-admin-key-policy-01` — Decide bootstrap admin key expiry/rotation policy and implement/docs accordingly. (90-day default TTL via `CONSCIENCE_BOOTSTRAP_KEY_TTL_HOURS`; ADR-023; 16/16 bootstrap tests pass; all 24 packages pass)
- [x] `api-rate-limit-scope-01` — Make API rate limiting scope-aware for admin/session/readonly/webhook keys.
- [x] `repo-hygiene-generated-artifacts-01` — Ignore/document generated local artifacts that pollute `git status`. (`.gitignore` updated: `.morty/`, `/conscience`, `/bin/`, `dev.db`, `*.db-*`, `**/runs/`; untracked `.axiom/state/materializer-state.json` + all `.morty/` files; finding documented in `.memory-bank/findings/`; commit `f6a1c1e`)

- [x] `schema-memory-01` — Draft consolidated SQL schema and memory ledger foundation from `specs/002-memory.md`, `specs/003-database.md`, `specs/005-security.md`, `specs/007-json-schema.md`, and `specs/011-canonical-definitions.md`. (15/16 ACs PASS; AC-MEM-016 pending PostgreSQL runtime verification by user)
- [x] `repo-bootstrap-01` — Scaffold Go module and repository layout from `specs/000-north-star.md`, `specs/001-architecture.md`, `specs/021-repository-layout.md`, and `specs/022-library-research.md`. (AC-1 through AC-5 PASS)

## Up Next

- [x] `runtime-harness-01` — Implement cognition loop, transaction staging, tool execution, and multi-turn planning. (Core loop proven via SQLite integration tests; sandbox/RLS/pg_cron deferred to deployment-ops-01)
- [x] `interfaces-api-cli-01` — Implement REST API, MCP server, CLI, OpenAPI contract, UI adapter, and user flows. (All 7 phases complete; 73 ACs all verified; ~185+ tests across all interface packages)
- [x] `deployment-ops-01` — Implement deployment, webhooks/events, HITL interrupt state, and subagent orchestration. (25/25 ACs covered; 43 tests; AC-DEP-04 horizontal scaling requires Postgres)
- [x] End-to-end integration test across schema → harness → API/CLI/MCP → deployment. ← **DONE** (3 E2E tests: AllSubsystems, ErrorRecoveryFlows, SessionLifecycle — all PASS)

## Completed

- [x] `schema-memory-01` — Consolidated SQL schema (15/16 ACs PASS)
- [x] `repo-bootstrap-01` — Scaffold Go module + repository layout (5/5 ACs PASS)
- [x] `spec-006-hardening-01` — Remediate SPEC-006 gaps: billing write path, circuit breaker persistence, async tool executor, stored procedure equivalents. (7/7 ACs PASS)
- [x] `spec-002-hardening-01` — Remediate SPEC-002 gaps: page resolution, dedup, model routing, reactive truncation, page-fault handler. (5/5 ACs PASS)
- [x] `spec-009-hardening-01` — Wire auto-migration and heartbeat into `cmd/conscience/main.go` startup (gaps found in sweep-012). (2/2 ACs PASS)
- [x] `spec-020-hardening-01` — Remediate SPEC-020 gaps found in sweep-021. Single long-running transaction, DB-persisted staging buffer, respond action, command type system, 60s timeout, crash recovery. All 11 items remediated.
- [x] `spec-013-hardening-01` — Webhook hardening: production migration, idempotency, routing loop, rate limiter, route wiring. (5/5 gaps remediated; 20 tests)
- [x] `spec-003-hardening-01` — Remediate SPEC-003 gaps: create_agent_memory_table(), soft_delete_intercept(), agent_budget_limits migration. (12 new tests, 2 migrations, 1 new Go package)
- [x] `spec-014-hardening-01` — HITL hardening: production migration, hitl.Manager wiring into main.go, API approval handlers refactored. (all 3 gaps remediated)
- [x] `spec-016-hardening-01` — CLI hardening: config priority chain, ServerFunc/InitFunc wired, migrate create writes files, config set handles nested keys, config edit added, missing serve flags. (all 8 gaps remediated)
- [x] `spec-017-hardening-01` — Shim architectural refactor: opencode shim calls native API service layer instead of raw DB. (all 8 gaps remediated; 30 shim tests pass)

## Active (Idle Sweep)

- [x] `idle-spec-conformance-sweep-01` `sweep-006` — `specs/002-memory.md`: GAPS FOUND → remediated via `spec-002-hardening-01`. Previously swept: 006 (gaps→remediated), 018 (conformant), 019 (conformant), 000 (conformant), 001 (conformant).
- [x] `idle-spec-conformance-sweep-01` `sweep-007` — `specs/003-database.md`: GAPS FOUND. 2 HIGH: `create_agent_memory_table` + `soft_delete_intercept` not executable; 1 MEDIUM: `staging_buffer`/`agent_budget_limits` missing migrations; 5 LOW-MEDIUM: SQLite parity, webhook/HITL migrations, JSON Schema tests, cache metrics. → Recommend `spec-003-hardening-01`.
- [x] `idle-spec-conformance-sweep-01` `sweep-008` — `specs/004-subagents.md`: CONFORMANT. All 5 ACs pass. 4 deferred minor gaps (Postgres trigger replaced by Go method, spawn procedure, RBAC scope model not implemented, agent_messages read path missing). No remediation work item — gaps are low-impact design choices.
- [x] `idle-spec-conformance-sweep-01` `sweep-009` — `specs/005-security.md`: CONFORMANT. All 15 checks pass: classifier, secrets inject/scrub, RLS role model (4 roles), append-only, vault isolation, table whitelist, multi-statement split. 4 minor gaps: DML_DELETE category not in Go classifier, secret_access_audit has no runtime writers, DDL not hard-gated at executor, no cognitive firewall scanner. All are pre-existing design decisions — no remediation work item.
- [x] `idle-spec-conformance-sweep-01` `sweep-010` — `specs/007-json-schema.md`: CONFORMANT. 14/14 checks: AgentOutput schema, parser (json.Unmarshal only), field validation, sanitization, classifier, whitelist, audit ledger, extensibility, test coverage — all PASS. 1 deferred medium: Structured Outputs API enforcement not yet wired (LLM SDKs are stubs). 3 deferred minor (pre-existing): no `task_update` field, no `justification` on ToolRequest, DML_DELETE not separated from DML_WRITE. Remaining: 12 specs (008, 009, 010, 011, 012, 013, 014, 015, 016, 017, 020, 022).
- [x] `idle-spec-conformance-sweep-01` `sweep-011` — `specs/008-harness.md`: CONFORMANT. 57/62 checks fully implemented. 5 deferred: secrets scrubbing path, FOR UPDATE SKIP LOCKED (Postgres), cache breakpoints (provider-dependent), model cascade routing, three-tier SQL execution. Core loop, heartbeat, tool execution, planning, error recovery, circuit breakers, context truncation, page-fault handler all verified. No remediation work item required.
- [x] `idle-spec-conformance-sweep-01` `sweep-012` — `specs/009-deployment.md`: GAPS FOUND. 2 HIGH: `AutoMigrate()` and `StartHeartbeatLoop()` not wired into `cmd/conscience/main.go` startup. 1 MEDIUM: `ClaimNextReadyTask()` missing `FOR UPDATE SKIP LOCKED` for concurrent Postgres. → Remediated via `spec-009-hardening-01` (2 wires added; FOR UPDATE SKIP LOCKED tracked as AC-DEP-04).
- [x] `idle-spec-conformance-sweep-01` `sweep-013` — `specs/010-tools.md`: CONFORMANT. 28/28 core checks pass: tool registry (tools_registry, custom_agent_tools), resolution order (internal → skill-linked → JIT → built-in), skills plugin system (skills_registry + progressive disclosure), async tool executor (goroutine poller with transaction-isolated claiming). 3 LOW gaps (test_results, trigger_tool_ci, tool_files trigger) — all Postgres-only PL/pgSQL. 6 deferred (RLS enforce_ownership, subprocess/HTTP stubs, event-driven plugins). No remediation work item required.
- [x] `idle-spec-conformance-sweep-01` `sweep-014` — `specs/011-canonical-definitions.md`: CONFORMANT. 13/13 sections aligned. 1 LOW gap (embedding_model enforcement — embedding path not yet wired). No remediation work item.
- [x] `idle-spec-conformance-sweep-01` `sweep-015` — `specs/012-system-prompt-and-discovery.md`: CONFORMANT. 21/24 checks pass: prompt assembly (6 layers), schema discovery (core + dynamic, Postgres + SQLite fallback), progressive disclosure (skills metadata only + load_skill), sub-agent memory isolation (compressed pointers fork), cache cost tracking (agent_billing with all 4 token types). 3 deferred: cache_control annotations (LLM stub dependency), cache invalidation rules (policy, not runtime), sub-agent tool filtering (tools not restricted to internal-only). No remediation work item required.
- [x] `idle-spec-conformance-sweep-01` `sweep-016` — `specs/013-webhooks-and-events.md`: GAPS FOUND. 1 HIGH: `external_events`/`webhook_registrations`/`routing_rules` tables have no production migration (test-only DDL). 1 MEDIUM: event routing trigger/Go equivalent not implemented (no automated routing or session wake). 3 LOW: no UNIQUE index for idempotency, no ON CONFLICT DO NOTHING, rate limiting constant exists but no runtime enforcement, webhook route not mounted in main.go HTTP tree. → Recommend `spec-013-hardening-01`.
- [x] `idle-spec-conformance-sweep-01` `sweep-017` — `specs/014-hitl-interrupt-state.md`: GAPS FOUND. 1 HIGH: `approval_requests`/`hitl_configuration`/`notification_log` tables have no production migration (test-only DDL). 2 MEDIUM: HITL Manager (`StartExpiryCron`, config init) not wired into `cmd/conscience/main.go`; API approval handlers use raw SQL instead of HITL Manager. 2 LOW: tool `requires_approval` check not wired into harness tool executor; circuit breaker→HITL pause not fully integrated. → Recommend `spec-014-hardening-01`.
- [x] `idle-spec-conformance-sweep-01` `sweep-018` — `specs/015-api-and-mcp.md`: CONFORMANT. 30/33 endpoint checks pass: all 8 endpoint families (sessions, memory, tasks, tools/skills, approvals, billing, config, auth). 163 tests (142 API + 21 MCP). 3 gaps: PATCH /api/v1/config (HIGH — deferred design), GET /api/v1/config/models (MEDIUM), GET /iterations/:iid/audit (LOW). 2 deferred: MCP stdio transport, Postgres LISTEN/NOTIFY. No remediation work item required.
- [x] `idle-spec-conformance-sweep-01` `sweep-019` — `specs/016-cli-interface.md`: GAPS FOUND. 5 HIGH: config priority chain broken (`--config` dead flag), interactive approval missing, 5 non-functional stubs, `status` bypasses formatter, 21 missing flags. 2 MEDIUM: approve/reject naming mismatch, migrate bare-command mismatch. 1 LOW: nested config key lookups. → Recommend `spec-016-hardening-01`.
- [x] `idle-spec-conformance-sweep-01` `sweep-020` — `specs/017-ui-adapter-layer.md`: GAPS FOUND. 1 CRITICAL, 5 HIGH, 4 MEDIUM, 6 LOW. Architectural violation (shim bypasses native API), 4/6 SSE event types missing, shim_session_map unused, 4 endpoints missing. → Recommend `spec-017-hardening-01`.
- [x] `idle-spec-conformance-sweep-01` `sweep-021` — `specs/020-multi-turn-planning.md`: GAPS FOUND. 4 HIGH, 5 MEDIUM, 2 LOW. No single long-running transaction, in-memory staging buffer, missing respond action, no command type system. → Recommend `spec-020-hardening-01`.
- [x] `idle-spec-conformance-sweep-01` `sweep-022` — `specs/021-repository-layout.md`: DEFERRED. All 22 dirs exist, dependency graph correct, Dockerfile/Makefile match. File consolidation differences are documentation mismatches. No remediation item.
- [x] `idle-spec-conformance-sweep-01` `sweep-023` — `specs/022-library-research.md`: DEFERRED. 4/10 deps match spec; lib/pq vs pgx/v5 is documented design choice; LLM/MCP SDKs are stub-deferred. All avoidance rules satisfied. CGO_ENABLED=0 works. No remediation item.

**ALL 22 SPECS SWEPT. IDLE SWEEP COMPLETE.**

## Gap Remediation Queue

- [x] `spec-013-hardening-01` — Remediate SPEC-013 gaps found in sweep-016: (1) production migration for webhook tables, (2) UNIQUE index + ON CONFLICT DO NOTHING for idempotency, (3) wire /webhooks/ route in main.go, (4) Go-level event routing loop with session wake, (5) token-bucket rate limiter at handler. (5/5 gaps remediated; 20 tests)
- [x] `spec-014-hardening-01` — Remediate SPEC-014 gaps found in sweep-017: (1) production migration for `approval_requests`, `hitl_configuration`, and `notification_log` tables, (2) wire `hitl.Manager.StartExpiryCron` and config init into `cmd/conscience/main.go`, (3) refactor API approval handlers to use `hitl.Manager` instead of raw SQL. (All 3 gaps remediated; harness test regressions fixed; 19/19 packages pass)
- [x] `spec-016-hardening-01` — Remediate SPEC-016 gaps found in sweep-019: (1) wire dead `--config` flag + add `~/.conscience/` and `/etc/` config fallbacks, (2) interactive approval already implemented, (3) wire 5 non-functional subcommand stubs (ServerFunc/InitFunc wired + migrate create writes files + config edit added), (4) status already produces structured JSON/YAML output, (5) add missing flags (--adapter, --migrations to serve), (6) approve/reject naming already aligned, (7) migrate already aliases to migrate run, (8) nested dot-notation already works in config get + fixed in config set. All 8 gaps remediated.
- [x] `spec-017-hardening-01` — Remediate SPEC-017 gaps found in sweep-020: (1) architectural refactor: shim now calls native API service layer instead of raw DB, (2) 4 SSE event types already implemented, (3) standalone /event endpoint wired, (4) PATCH /session/:id, GET /message/:messageID, PUT /auth/:id, PATCH /config already implemented, (5) shim_session_map wired + api_key in create response, (6) message ID bug fixed, (7) provider/agent wired to live state, (8) /project + /vcs registered as 501 stubs. All 8 gaps remediated.
- [x] `spec-020-hardening-01` — Remediate SPEC-020 gaps found in sweep-021: (1) single long-running transaction across all planning turns, (2) persisted staging buffer to database, (3) respond action for replying without commit, (4) command type system (sql/file/memory/tool_call_ref), (5) wire into harness runtime, (6) transaction timeout 60s, (7) crash recovery via goroutine, (8) process memory_state_changes at commit, (9) end_iteration concept, (10) enforce buffer limits, (11) tool_exec→planning state transition. (All 11 items remediated; 26 tests)
- [x] `spec-003-hardening-01` — Remediate SPEC-003 gaps found in sweep-007: (1) executable `create_agent_memory_table()` via migration 006 + Go package `internal/db/dynamic/`, (2) executable `soft_delete_intercept()` trigger (migration 006 + Go implementation), (3) `agent_budget_limits` migration (005). 12 new tests, 2 migration files, 1 new Go package. Build + all 23 packages pass.
- [x] `polish-phase` — Polish & Complete: test coverage expansion for CLI/MCP/Shim, web admin UI, spec-verifier hardening. Final state: CLI 75.0%, MCP 77.5%, Shim 67.6%, Memory 100%, Web 96.9%. Added 31 new tests: shim utility (toInt/toInt64/toFloat64/toBool/toString), emitShimEventForSession, sessionIDFromPerm, validateAuth (Bearer+Basic+edge cases), SSE handler startup, CLI config overrides with temp config files. Spec-004, Spec-010, Spec-012 all confirmed CONFORMANT. All 24 packages pass build+test+vet.

## Parked

- Web admin UI — Already built, 96.9% coverage, 14 tests, dark-themed dashboard with sessions/memory views
