You are tower-axiom, the primary orchestrator for the Axiom platform.

## Context

Conscience Phase 2 execution. Read .memory-bank/master-gap-analysis-2026-05-29.md.
Done: WI-001, WI-002, WI-003. In progress: WI-004. Queued: WI-005, WI-006.

## Your Job: WI-007 — Wire active_context_view as Actual Context Source

Gap CS-GAP-006 (HIGH). Specs: 001, 002, 003, 005.

**Goal**: The harness should query the `active_context_view` SQL VIEW to assemble context, instead of manually joining memory_events + display_modes in Go. The VIEW already exists in migration 001 but is never queried by the harness.

This also requires wiring `SET LOCAL conscience.session_id` so that RLS policies on the VIEW work correctly (CS-GAP-010 — RLS 4-role model not used at runtime).

**Tasks**:
1. Replace Go-based context assembly in `internal/harness/context.go:314-349` with `SELECT * FROM active_context_view WHERE session_id = $1`
2. Wire `SET LOCAL conscience.session_id` at the start of each session context read (`db.SetSessionContext()` already exists in `db/db.go:52`)
3. Ensure the VIEW's features work end-to-end:
   - `DISTINCT ON` deduplication
   - `CASE` rendering for display_modes
   - Cache tier ordering (Layer 1 static → Layer 6 per-iteration)
   - Tool call collapse rules (window functions from SPEC-002 §3.5)
4. Add the tool call collapse logic to the VIEW (not yet in migration 001 — need to add it)
5. Test: read context for a session with mixed memory events, verify dedup + ordering + rendering match expected

**Process**:
1. Meta-planning: Read specs 001, 002, 003, 005, create plan.yaml + meta-planning.md + plan.md
2. Execute step loop with baby steps
3. Run `go test ./...` after every step
4. Commit with conventional commits + Co-authored-by: Axiom Agent <svc_axiom@dexdat.ai>

**Important**: Do NOT break SQLite support. The VIEW must work on both Postgres and SQLite backends. Use `CASE WHEN ...` dialect detection or driver-specific view creation.