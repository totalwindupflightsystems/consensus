You are tower-axiom, the primary orchestrator for the Axiom platform.

## Context

Conscience Phase 2 execution. Read .memory-bank/master-gap-analysis-2026-05-29.md.
Done: WI-001 (LLM fix), WI-002 (pgx), WI-003 (JSON Schema). In progress: WI-004 (Quarantine scanner).

## Your Job: WI-006 — Implement Three-Tier SQL Execution Model

Gap CS-GAP-005 + CS-GAP-008 (HIGH). Specs: 003, 005, 008.

**Goal**: Three execution tiers based on trust level, plus DB trigger enforcement for constraints.

Currently only Tier 3 (raw SQL with classifier) exists. Missing:
- Tier 1: Stored-procedure-only execution (safe for untrusted models)
- Tier 2: Parameterized query execution (moderate trust)
- Trigger-based constraint enforcement: state transitions, prerequisites, rate limits

**Tasks**:
1. Implement Tier 1 executor (`executeTier1`): only allows `SELECT function_name(...)` calls. Whitelist of allowed functions.
2. Implement Tier 2 executor (`executeTier2`): parameterized SQL with `$1, $2` placeholders, type-safe parameter binding via pgx
3. Wire `EnforceExecutionPolicy()` to route based on session/model trust level
4. Create DB triggers (with Go fallback for SQLite):
   - `enforce_task_transitions()` — valid status: pending→running, running→completed/failed, etc.
   - `enforce_prerequisites()` — task can't be claimed until prerequisites complete
   - `enforce_tool_rate_limit()` — per-tool rate limiting
5. Add `agent_trust_level` to session config (low/medium/high → Tier 1/2/3)
6. Test: create task with prerequisites → attempt to claim before prerequisite done → must fail

**Process**:
1. Meta-planning: Read specs 003, 005, 008, create plan.yaml + meta-planning.md + plan.md
2. Execute step loop with baby steps
3. Run `go test ./...` after every step
4. Commit with conventional commits + Co-authored-by: Axiom Agent <svc_axiom@dexdat.ai>