You are tower-axiom. Implement budget enforcement in the Conscience project so sessions pause when they exceed their budget_limit_cents.

BUG: Sessions with budget_limit_cents=1 complete normally without being paused. The budget field is accepted in the API but never enforced.

WHAT TO IMPLEMENT:

1. In internal/harness/executor.go (or planning.go), add a budget check in the planning loop that:
   - Tracks the cumulative cost (sum of prompt_tokens + completion_tokens * cost per token)
   - Compares against the session's budget_limit_cents
   - Transitions session to 'paused' when budget is exceeded

2. In the session creation flow (internal/session/ or internal/api/):
   - Ensure budget_limit_cents is stored in the sessions table
   - Check if the column exists in the schema; if not, add it via a migration or ALTER TABLE

3. In the planning loop (planning.go's runInteractivePlanning):
   - After each LLM call, calculate cost (prompt_tokens * INPUT_PRICE + completion_tokens * OUTPUT_PRICE)
   - Accumulate total_cost_cents
   - After accumulating cost, check if total_cost_cents > budget_limit_cents
   - If exceeded: set status = 'paused', insert approval request with "budget exceeded" reason
   - Use approximate pricing: $0.50/M input tokens, $2.00/M output tokens (DeepSeek pricing)

4. In the session creation handler:
   - Accept budget_limit_cents field
   - Store it in the sessions table
   - If column missing, add it

VERIFICATION:
- Run `go build -buildvcs=false ./...`
- Only modify files in internal/harness/ and internal/session/ or internal/api/
- Keep it simple — minimal implementation that passes AC verification

APPROACH: Start with the simplest path. Add budget_limit_cents to the session struct if missing. Add cost accumulation in the planning loop. Add the pause transition when budget exceeded.
