You are tower-axiom, the primary orchestrator for the Axiom platform.

## Context

Conscience Phase 2 execution. Read .memory-bank/master-gap-analysis-2026-05-29.md. WI-003 (JSON Schema) done, WI-004 (Quarantine scanner) in progress.

## Your Job: WI-005 — Implement External Tool Execution Sandbox

Gap CS-GAP-004 (CRITICAL). Specs: 010, 014.

**Goal**: External (non-SQL) tools registered in `custom_agent_tools` can actually execute in an isolated sandbox. Currently the table exists but `ExecuteExternalTool()` is not implemented.

**Tasks**:
1. Implement `ExecuteExternalTool()` in `internal/tools/` — for MVP use subprocess execution with:
   - Timeout enforcement (30s default)
   - Output size limit (1MB)
   - Working directory isolation (temp dir per execution)
   - Environment variable whitelist (CONSCIENCE_*, HOME, PATH, USER)
2. Wire into harness: when agent outputs `tool_call_ref`, the harness executor calls `ExecuteExternalTool()`
3. Capture results → `tool_results` table (output, exit_code, duration, error)
4. Add per-tool rate limiting (column `rate_limit_per_min` exists in schema, wire enforcement)
5. Add tool approval gating: if `tools_registry.requires_approval=true`, create HITL approval request before execution
6. Test: register a tool, execute it via harness, verify output captured

**Security constraints**:
- No network access for untrusted tools (configurable)
- Max 10 concurrent tool executions
- Tool output sanitized before writing to memory_events

**Process**:
1. Meta-planning: Read specs 010, 014, create plan.yaml + meta-planning.md + plan.md
2. Execute using step loop with baby steps
3. Run `go test ./...` after every step
4. Commit with conventional commits + Co-authored-by: Axiom Agent <svc_axiom@dexdat.ai>