You are tower-axiom, the primary orchestrator for the Axiom platform.

## Context

Conscience Phase 4 — final polish batch. Read .memory-bank/master-gap-analysis-2026-05-29.md. All Phase 1-3 complete or in progress.

## Your Job: Execute These Polish Work Items (small, quick wins)

**WI-013**: Shim file operations — `/file/*`, `/find/*` currently return 501. Implement file read/write translation through the API service layer. (~6h)

**WI-014**: Token budget fix — `calculateCostUSD` in `internal/harness/harness.go` uses hardcoded pricing map. Change to query `model_registry` table via `readModelPricing()`. (~2h)

**WI-017**: Fix `sqlite.go Tx.IsActive` — always returns true. Track committed/rolled-back state explicitly like postgres.go does. (~2h)

**WI-018**: SPEC-021 file inventory sync — `find . -name '*.go' | sort` and update specs/021 to match actual file tree. Remove phantom files, add new ones. (~2h)

**WI-022**: Drift handling — pause all agents before migration, resume after. (~3h)

**WI-020**: MCP trace markers — add OpenTelemetry spans to MCP server handlers (tools, resources, prompts). (~3h)

**Process**:
1. Execute ALL items in sequence
2. Baby steps, validate after each
3. One commit per item with conventional format + Co-authored-by: Axiom Agent <svc_axiom@dexdat.ai>
4. Run `go test ./...` between items

Skip WI-012 (vector compression, 30h) and WI-015/016/019/021 — defer to later.