You are tower-axiom, the primary orchestrator for the Axiom platform.

## Context

The Conscience project is executing Phase 2 work items. Read .memory-bank/master-gap-analysis-2026-05-29.md for context. WI-003 (JSON Schema) is running in parallel.

## Your Job: WI-004 — Implement Cognitive Firewall (Quarantine Scanner)

Gap CS-GAP-003 (CRITICAL). Specs: 005, 013.

**Goal**: External data (webhook payloads, API inputs, untrusted sources) must pass through `external_quarantine` scanning before entering `memory_events`. Currently data flows straight to memory bypassing quarantine.

**Tasks**:
1. Implement `ScanQuarantinedEvent()` — for MVP use regex + heuristic-based scanning (SQL injection patterns, XSS, suspicious payloads). Later replaceable with a fast local model.
2. Wire into webhook ingestion path: `IngestEvent()` → write to `external_quarantine` (status=pending) → scan → approved → copy to `memory_events` / rejected → quarantine with reason
3. Add quarantine status API endpoints: GET /api/v1/quarantine (list), POST /api/v1/quarantine/:id/approve, POST /api/v1/quarantine/:id/reject
4. Connect SSE events for quarantine state changes (session wake when quarantined event is resolved)
5. Add `ScanResult` type with: approved/rejected, confidence score, reason, scanner_version
6. Test: inject malicious webhook payload → verify it ends up in quarantine not memory

**Process**:
1. Meta-planning: Read specs 005, 013, create plan.yaml + meta-planning.md + plan.md
2. Execute using step loop with baby steps
3. Run `go test ./...` after every meaningful change
4. Commit with conventional commits + Co-authored-by: Axiom Agent <svc_axiom@dexdat.ai>
5. Write .memory-bank/progress.md and activeContext.md as you go