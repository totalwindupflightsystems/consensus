work_item: add-missing-tables
title: "Add remaining missing SQLite tables (skills_registry, tool_results, etc.)"
priority: medium
status: pending
files_to_modify:
  - internal/migrate/migrations/009_sqlite_task_tool_tables.sql
steps:
  - step: 1
    action: "Add skills_registry table to migration 009"
    details: "CREATE TABLE IF NOT EXISTS skills_registry matching the schema in 001_initial_schema.sql"
  - step: 2
    action: "Add tool_results table to migration 009"
    details: "CREATE TABLE IF NOT EXISTS tool_results matching the schema in 001_initial_schema.sql"
verification:
  - "All referenced tables exist after migration"
  - "ReadActiveContext completes without SQL errors"
