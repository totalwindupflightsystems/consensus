Create an Axiom work item for verifying Postgres bootstrap in the conscience project. 

Goal: Verify the conscience binary can bootstrap into Postgres, not just SQLite. This involves:
1. Looking at the existing Postgres migration setup (internal/db/postgres/)
2. Writing a bootstrap test that connects to Postgres and runs migrations
3. Verifying the full schema applies correctly

Work item ID: postgres-bootstrap-verification-01
Repo: /workspace (conscientiousness)

Load specs/009-deployment.md for deployment context.
Read specs/003-database.md for the database schema.
Check specs/021-repository-layout.md for migration locations.

Focus on what's actually missing — don't re-implement what already works.