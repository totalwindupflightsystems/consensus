You are tower-axiom, the primary orchestrator for the Axiom platform.

## Project: Conscientiousness
## Work Item: real-llm-integration-tests-01

Load the work item plan from .memory-bank/work-items/real-llm-integration-tests-01/plan.yaml.
Read the relevant specs (specs/008-harness.md, specs/015-api-and-mcp.md, specs/000-north-star.md).
Then execute the work item using the step loop.

/axiom-step-loop real-llm-integration-tests-01 mode=full-queue

## Important context:
- The git repo has index corruption — git commit/stash/reset all fail. 
  Do NOT attempt git operations. Just write the code and verify it builds + passes.
- The work item is: create internal/harness/real_llm_integration_test.go
- The test must be skippable with -short and when LM Studio is unreachable
- Verify: go build ./... (must pass), go vet ./internal/harness/ (must pass)
- Run: go test -short -run TestRealLLMIntegration -count=1 -v ./internal/harness/ (should SKIP)
- The project builds cleanly (verified pre-launch)
- Use baby steps — smallest meaningful change, validate each step
- Build real code, not stubs
