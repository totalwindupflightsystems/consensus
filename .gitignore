# Axiom (managed)
.worktrees/*/
.axiom/sessions/
.axiom/state/
_tmp/
.graph-harness/
.morty/

# Secrets — NEVER commit .env
.env

# Build artifacts
/consensus
/bin/
dev.db
*.db-shm
*.db-wal

# Work item — ephemeral test run output
**/runs/
dev.db*
.worktrees/

# Credentials — NEVER commit these
.env
.auth.json
*_credentials.json
*_secrets.yaml
service-account*.json
*.pem
!*.pub.pem
